import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  deviceListWrap: {
    position: 'relative',
    width: '100%',
    flex: 1,
    display: 'flex'
  },
})

export default styles

